package chess.gameState;

public class FigureFactory {

    /*public static Figure createFigure(FigureType type, FigureColor color) {
        if (type == FigureType.PAWN) {
            return new Pawn(color == FigureColor.WHITE);
        }
        if (type == FigureType.KING) {
            return new King(color == FigureColor.WHITE);
        }
        // TODO complete
    }*/
}
